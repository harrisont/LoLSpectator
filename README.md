# LoLSpectator
Spectate LoL games

## Setup
1. `pip install pip-tools`
2. `pip-sync`

## Development

### Update Requiremnets
1. Change the requirements in `requirements.in`.
2. `pip-compile`
3. `pip-sync`
